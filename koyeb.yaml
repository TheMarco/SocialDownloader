# Example koyeb.yaml

# Optional: Specify environment variables if needed
# env:
#   - key: MY_VAR
#     value: "my_value"

# Optional: Specify build command if needed (often not required for standard Python)
# build_command: "echo 'Custom build step'"

# Optional: Specify run command if different from default (e.g., Procfile or standard detection)
# run_command: "gunicorn run:app --bind 0.0.0.0:$PORT"

build:
  # Specify system packages needed during the build and runtime
  # Uses apt package manager (Debian/Ubuntu base)
  system_packages:
    - ffmpeg # Add ffmpeg here
    # - other-package # Add any other system packages you might need
